import { JwtSignUpView } from 'src/auth/view/jwt';

// ----------------------------------------------------------------------

export const metadata = { title: `Sign up : NovelSync` };

export default function Page() {
  return <JwtSignUpView />;
}
